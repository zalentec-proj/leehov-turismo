"use client";
/* eslint-disable @typescript-eslint/no-unused-expressions -- Sonner feedback is selected by the settings action result. */

import { Save } from "lucide-react";
import { useState, useTransition } from "react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Switch } from "@/components/ui/switch";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Textarea } from "@/components/ui/textarea";
import type { MediaAsset } from "@/features/media/types";
import { saveSiteSettingsAction } from "@/features/settings/actions";
import type { AdminSiteSettings, SiteSettingsFormValues } from "@/features/settings/types";
import type { GoogleBusinessSettings } from "@/features/testimonials/types";

export function AdminSettingsForm({ settings, media, google }: { settings: AdminSiteSettings; media: MediaAsset[]; google: GoogleBusinessSettings }) {
  const [form, setForm] = useState<SiteSettingsFormValues>({ contact: settings.contact, whatsapp: settings.whatsapp, social: settings.social, home: settings.home, seo: { siteName: settings.seo.siteName, titleTemplate: settings.seo.titleTemplate, defaultDescription: settings.seo.defaultDescription, ogImageAssetId: settings.seo.ogImageAssetId }, email: settings.email, tracking: settings.tracking, consent: settings.consent });
  const [pending, startTransition] = useTransition();
  const input = (label: string, value: string, onChange: (value: string) => void, placeholder = "") => <Field label={label}><Input value={value} onChange={(event) => onChange(event.target.value)} placeholder={placeholder} /></Field>;
  return <div className="space-y-6"><Tabs defaultValue="contact"><TabsList className="h-auto flex-wrap justify-start"><TabsTrigger value="contact">Contato</TabsTrigger><TabsTrigger value="whatsapp">WhatsApp</TabsTrigger><TabsTrigger value="social">Redes sociais</TabsTrigger><TabsTrigger value="home">Home</TabsTrigger><TabsTrigger value="seo">SEO global</TabsTrigger><TabsTrigger value="email">E-mails</TabsTrigger><TabsTrigger value="analytics">Analytics</TabsTrigger><TabsTrigger value="google">Google Business</TabsTrigger></TabsList><SettingsTab value="contact" title="Contato e endereço" description="Informações públicas usadas no rodapé e na página de contato."><div className="grid gap-4 md:grid-cols-2">{input("Telefone", form.contact.phone, (phone) => setForm({ ...form, contact: { ...form.contact, phone } }))}{input("E-mail de contato", form.contact.contactEmail, (contactEmail) => setForm({ ...form, contact: { ...form.contact, contactEmail } }))}<div className="md:col-span-2"><Field label="Endereço"><Textarea value={form.contact.address} onChange={(event) => setForm({ ...form, contact: { ...form.contact, address: event.target.value } })} /></Field></div></div></SettingsTab><SettingsTab value="whatsapp" title="WhatsApp e mensagens" description="O CTA aparece somente quando houver um número institucional."><div className="grid gap-4 md:grid-cols-2">{input("Número com DDD", form.whatsapp.number, (number) => setForm({ ...form, whatsapp: { ...form.whatsapp, number } }), "+55 11 99999-9999")}<div />{input("Mensagem geral", form.whatsapp.defaultMessage, (defaultMessage) => setForm({ ...form, whatsapp: { ...form.whatsapp, defaultMessage } }))}{input("Mensagem de caravana", form.whatsapp.caravanMessage, (caravanMessage) => setForm({ ...form, whatsapp: { ...form.whatsapp, caravanMessage } }))}</div></SettingsTab><SettingsTab value="social" title="Redes sociais" description="Use URLs HTTPS completas."><div className="grid gap-4 md:grid-cols-3">{input("Instagram", form.social.instagram, (instagram) => setForm({ ...form, social: { ...form.social, instagram } }))}{input("Facebook", form.social.facebook, (facebook) => setForm({ ...form, social: { ...form.social, facebook } }))}{input("YouTube", form.social.youtube, (youtube) => setForm({ ...form, social: { ...form.social, youtube } }))}</div></SettingsTab><SettingsTab value="home" title="Home e vídeo institucional" description="Textos aplicados à seção de depoimentos aprovada no Paper."><div className="grid gap-4 md:grid-cols-2">{input("URL do vídeo", form.home.videoUrl, (videoUrl) => setForm({ ...form, home: { ...form.home, videoUrl } }))}{input("Chamada dos depoimentos", form.home.testimonialsEyebrow, (testimonialsEyebrow) => setForm({ ...form, home: { ...form.home, testimonialsEyebrow } }))}{input("Título dos depoimentos", form.home.testimonialsTitle, (testimonialsTitle) => setForm({ ...form, home: { ...form.home, testimonialsTitle } }))}</div></SettingsTab><SettingsTab value="seo" title="SEO global" description="Fallbacks usados quando uma página não define metadados próprios."><div className="grid gap-4 md:grid-cols-2">{input("Nome do site", form.seo.siteName, (siteName) => setForm({ ...form, seo: { ...form.seo, siteName } }))}{input("Template de título", form.seo.titleTemplate, (titleTemplate) => setForm({ ...form, seo: { ...form.seo, titleTemplate } }), "%s | Leehov Turismo")}<div className="md:col-span-2"><Field label="Descrição padrão"><Textarea value={form.seo.defaultDescription} onChange={(event) => setForm({ ...form, seo: { ...form.seo, defaultDescription: event.target.value } })} /></Field></div><Field label="Imagem Open Graph"><Select value={form.seo.ogImageAssetId || "none"} onValueChange={(value) => setForm({ ...form, seo: { ...form.seo, ogImageAssetId: value === "none" ? "" : value } })}><SelectTrigger className="w-full"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="none">Sem imagem</SelectItem>{media.map((item) => <SelectItem key={item.id} value={item.id}>{item.fileName}</SelectItem>)}</SelectContent></Select></Field></div></SettingsTab><SettingsTab value="email" title="E-mails" description="A API key e o endereço técnico de envio continuam somente no servidor."><div className="grid gap-4 md:grid-cols-2"><Toggle label="Envios habilitados" checked={form.email.enabled} onChange={(enabled) => setForm({ ...form, email: { ...form.email, enabled } })} /><Toggle label="Confirmação ao visitante" checked={form.email.visitorConfirmationsEnabled} onChange={(visitorConfirmationsEnabled) => setForm({ ...form, email: { ...form.email, visitorConfirmationsEnabled } })} />{input("Destinatários de contato", form.email.contactRecipients.join(", "), (value) => setForm({ ...form, email: { ...form.email, contactRecipients: value.split(",").map((item) => item.trim()).filter(Boolean) } }), "equipe@exemplo.com")}{input("Destinatários de leads", form.email.leadRecipients.join(", "), (value) => setForm({ ...form, email: { ...form.email, leadRecipients: value.split(",").map((item) => item.trim()).filter(Boolean) } }))}{input("Nome do remetente", form.email.senderName, (senderName) => setForm({ ...form, email: { ...form.email, senderName } }))}{input("Reply-to", form.email.replyTo, (replyTo) => setForm({ ...form, email: { ...form.email, replyTo } }))}{input("WhatsApp no e-mail", form.email.whatsapp, (whatsapp) => setForm({ ...form, email: { ...form.email, whatsapp } }))}{input("Rodapé", form.email.footerText, (footerText) => setForm({ ...form, email: { ...form.email, footerText } }))}</div></SettingsTab><SettingsTab value="analytics" title="Analytics e consentimento" description="IDs validados; nenhum HTML ou script arbitrário é armazenado."><div className="grid gap-4 md:grid-cols-3">{input("GA4", form.tracking.gaMeasurementId, (gaMeasurementId) => setForm({ ...form, tracking: { ...form.tracking, gaMeasurementId } }), "G-XXXXXXXX")}{input("Google Tag Manager", form.tracking.gtmContainerId, (gtmContainerId) => setForm({ ...form, tracking: { ...form.tracking, gtmContainerId } }), "GTM-XXXXXXX")}{input("Meta Pixel", form.tracking.metaPixelId, (metaPixelId) => setForm({ ...form, tracking: { ...form.tracking, metaPixelId } }))}</div><div className="mt-6 grid gap-4 md:grid-cols-3"><Toggle label="Banner de consentimento" checked={form.consent.enabled} onChange={(enabled) => setForm({ ...form, consent: { ...form.consent, enabled } })} />{input("Versão", String(form.consent.version), (version) => setForm({ ...form, consent: { ...form.consent, version: Number(version) } }))}{input("Validade em dias", String(form.consent.durationDays), (durationDays) => setForm({ ...form, consent: { ...form.consent, durationDays: Number(durationDays) } }))}</div></SettingsTab><SettingsTab value="google" title="Google Business Profile" description="Conexão segura, localização confirmada e estado da sincronização."><div className="grid gap-4 md:grid-cols-3"><Status label="Basic API Access" value={google.apiAccessStatus === "approved" ? "Aprovado" : "Pendente"} /><Status label="OAuth" value={google.connection ? "Conectado" : google.credentialsConfigured ? "Pronto para conectar" : "Credenciais incompletas"} /><Status label="Localização" value={google.connection?.locationName || "Não confirmada"} /></div><div className="mt-5 rounded-xl border border-leehov-border bg-leehov-surface p-5 text-sm leading-6 text-leehov-muted"><p>Refresh tokens ficam criptografados no Supabase e access tokens vivem somente em memória. O app permanece em Testing, portanto pode exigir reconexão a cada sete dias.</p><div className="mt-4 flex flex-wrap gap-2">{!google.connection ? <Button asChild disabled={!google.credentialsConfigured}><a href="/api/google-business/connect">Conectar Google</a></Button> : <Button variant="outline" asChild><a href="/admin/depoimentos">Gerenciar avaliações</a></Button>}</div></div></SettingsTab></Tabs><div className="sticky bottom-4 flex justify-end"><Button size="lg" disabled={pending} className="bg-leehov-blue-600 shadow-lg" onClick={() => startTransition(async () => { const result = await saveSiteSettingsAction(form); result.success ? toast.success(result.message) : toast.error(result.message); if (result.success) window.location.reload(); })}><Save />{pending ? "Salvando…" : "Salvar configurações"}</Button></div></div>;
}

function SettingsTab({ value, title, description, children }: { value: string; title: string; description: string; children: React.ReactNode }) { return <TabsContent value={value}><Card className="mt-5 rounded-[18px] border-leehov-border p-6 shadow-leehov-card"><h3 className="text-xl font-bold text-leehov-navy-950">{title}</h3><p className="mt-2 mb-6 text-sm text-leehov-muted">{description}</p>{children}</Card></TabsContent>; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <div className="space-y-2"><Label>{label}</Label>{children}</div>; }
function Toggle({ label, checked, onChange }: { label: string; checked: boolean; onChange: (checked: boolean) => void }) { return <label className="flex min-h-9 items-center gap-3 rounded-lg border border-leehov-border px-3"><Switch checked={checked} onCheckedChange={onChange} />{label}</label>; }
function Status({ label, value }: { label: string; value: string }) { return <div className="rounded-xl border border-leehov-border p-4"><p className="text-xs font-bold uppercase tracking-[0.12em] text-leehov-muted">{label}</p><p className="mt-2 font-semibold text-leehov-navy-950">{value}</p></div>; }
